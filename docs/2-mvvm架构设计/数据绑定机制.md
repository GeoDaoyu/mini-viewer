# 数据绑定机制

数据绑定是 MVVM 架构的核心，它实现了数据模型和视图之间的自动同步。

## 响应式系统

### 基本概念

```typescript
class Observable<T> {
  private value: T
  private subscribers: Array<(value: T) => void> = []
  
  constructor(initialValue: T) {
    this.value = initialValue
  }
  
  get(): T {
    return this.value
  }
  
  set(newValue: T): void {
    if (this.value !== newValue) {
      this.value = newValue
      this.notify()
    }
  }
  
  subscribe(callback: (value: T) => void): void {
    this.subscribers.push(callback)
  }
  
  private notify(): void {
    for (const callback of this.subscribers) {
      callback(this.value)
    }
  }
}
```

### 在 GIS 中的应用

```typescript
class MapViewModel {
  // 响应式属性
  center = new Observable<[number, number]>([0, 0])
  zoom = new Observable<number>(0)
  rotation = new Observable<number>(0)
  
  // 计算属性
  get extent(): Extent {
    return this.calculateExtent(this.center.get(), this.zoom.get())
  }
  
  // 方法
  panTo(center: [number, number]): void {
    this.center.set(center)
  }
  
  zoomTo(zoom: number): void {
    this.zoom.set(zoom)
  }
}
```

## 属性访问器模式

使用 `@geodaoyu/accessor` 库实现属性访问器：

```typescript
import { declared, property } from '@geodaoyu/accessor'

class Map extends declared() {
  @property()
  declare layers: Layer[]
  
  @property()
  declare spatialReference: SpatialReference
  
  @property()
  declare extent: Extent
  
  constructor(options?: MapOptions) {
    super()
    this.layers = options?.layers || []
    this.spatialReference = options?.spatialReference || SpatialReference.WebMercator
  }
}
```

## 视图绑定

### 自动渲染

```typescript
class MapView {
  private viewModel: MapViewModel
  
  constructor(viewModel: MapViewModel, container: HTMLElement) {
    this.viewModel = viewModel
    this.container = container
    
    // 订阅视图模型变化
    this.viewModel.center.subscribe(() => this.render())
    this.viewModel.zoom.subscribe(() => this.render())
    this.viewModel.rotation.subscribe(() => this.render())
  }
  
  render(): void {
    // 使用视图模型的当前状态进行渲染
    const center = this.viewModel.center.get()
    const zoom = this.viewModel.zoom.get()
    // ... 渲染逻辑
  }
}
```

### 双向绑定

```typescript
class MapInteraction {
  private viewModel: MapViewModel
  private canvas: HTMLCanvasElement
  
  constructor(viewModel: MapViewModel, canvas: HTMLCanvasElement) {
    this.viewModel = viewModel
    this.canvas = canvas
    
    this.setupMouseEvents()
    this.setupTouchEvents()
    this.setupKeyboardEvents()
  }
  
  private setupMouseEvents(): void {
    this.canvas.addEventListener('wheel', (event) => {
      const delta = event.deltaY > 0 ? -1 : 1
      const newZoom = this.viewModel.zoom.get() + delta
      this.viewModel.zoom.set(newZoom)
      event.preventDefault()
    })
    
    this.canvas.addEventListener('mousedown', (event) => {
      // 处理拖拽逻辑
      const startX = event.clientX
      const startY = event.clientY
      const startCenter = this.viewModel.center.get()
      
      const onMouseMove = (moveEvent: MouseEvent) => {
        const dx = moveEvent.clientX - startX
        const dy = moveEvent.clientY - startY
        const newCenter = this.calculateNewCenter(startCenter, dx, dy)
        this.viewModel.center.set(newCenter)
      }
      
      const onMouseUp = () => {
        document.removeEventListener('mousemove', onMouseMove)
        document.removeEventListener('mouseup', onMouseUp)
      }
      
      document.addEventListener('mousemove', onMouseMove)
      document.addEventListener('mouseup', onMouseUp)
    })
  }
}
```

## 性能优化

### 批量更新

```typescript
class BatchUpdate {
  private updates: Array<() => void> = []
  private scheduled = false
  
  schedule(update: () => void): void {
    this.updates.push(update)
    
    if (!this.scheduled) {
      this.scheduled = true
      requestAnimationFrame(() => this.flush())
    }
  }
  
  private flush(): void {
    const updates = this.updates
    this.updates = []
    this.scheduled = false
    
    for (const update of updates) {
      update()
    }
  }
}
```

### 防抖处理

```typescript
function debounce<T extends (...args: any[]) => any>(
  func: T,
  wait: number
): (...args: Parameters<T>) => void {
  let timeout: number | null = null
  
  return (...args: Parameters<T>) => {
    if (timeout) {
      clearTimeout(timeout)
    }
    
    timeout = setTimeout(() => {
      func(...args)
      timeout = null
    }, wait) as unknown as number
  }
}

// 使用示例
const debouncedRender = debounce(() => view.render(), 16) // 约60fps
viewModel.center.subscribe(debouncedRender)
```

## 实际应用

在 Mini-Viewer 中，数据绑定机制用于：
- 地图状态（中心点、缩放级别）的响应式更新
- 图层可见性的自动同步
- 用户交互的实时反馈
- 性能优化的批量渲染
